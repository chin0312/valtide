"""Valtide backend — orchestration & API layer.

Statistical logic lives in the quant runtime; this package only fetches data,
normalizes it, runs pre-fitted parameters, validates, and serves the result.
"""

__version__ = "0.1.0"
